import { toInt } from 'importer-web-page/helpers/to-int';
import { module, test } from 'qunit';

module('Unit | Helper | to int');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toInt([42]);
  assert.ok(result);
});
