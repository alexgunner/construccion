import { multiplication } from 'importer-web-page/helpers/multiplication';
import { module, test } from 'qunit';

module('Unit | Helper | multiplication');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = multiplication([42]);
  assert.ok(result);
});
